# Dependensi server (Flask). Deteksi hardware dilakukan collector di laptop
# karyawan, jadi psutil/py-cpuinfo TIDAK perlu di server.
Flask==3.0.3
openpyxl==3.1.5
gunicorn==23.0.0        # WSGI server untuk produksi (di belakang nginx)
